dist: xenial
language: cpp

compiler: clang

services:
  - docker
  
before_install:
- docker pull rabits/qt:5.12-desktop
- docker run rabits/qt:5.12-desktop

script:
  - qmake mobata.pro
  - make
  - make check