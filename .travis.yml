dist: xenial
language: cpp

compiler: clang

services:
  - docker
  
before_install:
- docker pull rabits/qt:5.12-desktop
- docker run rabits/qt:5.12-desktop

before_script:
  - qmake -spec linux-clang

script:
  - make