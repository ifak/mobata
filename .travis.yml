dist: xenial
language: cpp

compiler: clang

before_install:
 - sudo apt-get update
 - sudo apt-get install qt5-default qtbase5-dev qttools5-dev graphviz-dev libqt5qml5 libqt5quick5 libqt5quickwidgets5

before_script:
  - qmake -spec linux-clang

script:
  - make
  - make
  - make check