dist: xenial
language: cpp

compiler: clang

before_install:
 - sudo apt-get update
 - sudo apt-get install qt5-default qtbase5-dev qttools5-dev qml
before_script:
  - qmake -spec linux-clang

script:
  - make